packages:
  - 'packages/*'
  - 'apps/*'

# Native and platform binaries this project trusts to run install scripts.
# better-sqlite3 compiles (or downloads) a native addon; esbuild fetches its binary.
allowBuilds:
  better-sqlite3: true
  esbuild: true
